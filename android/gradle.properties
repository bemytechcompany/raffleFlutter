org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Flutter avisa de que aplicar el Kotlin Gradle Plugin a mano romperá en
# versiones futuras y recomienda `builtInKotlin`. Se probó y hoy rompe al revés:
# el Kotlin que trae Flutter integrado es más antiguo que el que exige
# share_plus, y `:share_plus:compileDebugKotlin` falla.
# Se mantiene el KGP explícito (2.2.20, en settings.gradle) hasta que la
# versión integrada alcance a los plugins.
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
